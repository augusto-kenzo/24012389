#include <stdio.h>

int main(){

    int a, b, c;

    printf("Digite tres numeros inteiros positivos:");
    scanf("%d %d %d", &a, &b, &c);
    printf("A ordem inversa dos seus numeros sao:%d,%d,%d", c,b,a);

    return 0;
}
